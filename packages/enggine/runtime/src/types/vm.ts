// VM types are re-exported from ../vm/index.ts — import from there or directly from "@l8b/vm".
export type {
	ErrorInfo,
	GlobalAPI,
	MetaFunctions,
	SystemAPI,
	VMContext,
	VMWarnings,
	WarningInfo,
} from "@l8b/vm";
