{
	"installCommand": "corepack enable && corepack prepare pnpm@10.24.0 --activate && cd ../../.. && pnpm install",
	"buildCommand": "pnpm run build",
	"outputDirectory": ".l8b",
	"framework": null,
	"rewrites": [
		{
			"source": "/(.*)",
			"destination": "/index.html"
		}
	]
}