# npm configuration for publishing

# Use exact versions (no caret/tilde)
save-exact=true

# Require all dependencies to be explicitly listed
strict-peer-dependencies=false

# Use workspace protocol for local dependencies
# workspaces are resolved to the local package version

# Publish public packages
access=public

# Require NPM_TOKEN for authentication (from CI environment)
# This is set in GitHub Actions secrets
